import React from 'react';

import Button from './Button';

const TertiaryButton = (props) => {
  return (
    <Button {...props} className='bp-btn-tertiary' />
  )
};

export default TertiaryButton;
